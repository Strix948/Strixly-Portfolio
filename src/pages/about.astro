<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>About | Will (Strix/Strixly)</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700&family=Poppins:wght@700&family=Fira+Mono:wght@500&family=Nothing+You+Could+Do&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/style.css" />
    <style>
      :root {
        --main-bg: #000000;
        --main-bg2: #1a1027;
        --accent-main: #E07BE0;
        --accent-dark: #392F5A;
        --text-main: #E2E2E2;
        --border-main: #E2E2E2;
        --glow-main: #E07BE0;
      }
      body {
        font-family: 'Inter', Arial, sans-serif;
        background: linear-gradient(160deg, var(--main-bg) 0%, var(--main-bg2) 100%);
        transition: background 2s;
        color: var(--text-main);
        margin: 0;
        padding: 0;
        min-height: 100vh;
        position: relative;
        overflow-x: hidden;
      }
      .main-nav {
        position: sticky;
        top: 0;
        width: 100%;
        z-index: 100;
        background: var(--main-bg);
        border-bottom: 2.5px solid var(--accent-dark);
        box-shadow: 0 1px 12px 0 #0008;
        padding: 0;
        margin-bottom: 3em;
        transition: background 0.7s, border-color 0.4s;
      }
      .nav-content {
        display: flex;
        align-items: center;
        justify-content: space-between;
        max-width: 880px;
        padding: 0.12em 2em 0.3em 2em;
        margin: 0 auto;
      }
      .nav-brand {
        font-family: 'Nothing You Could Do', cursive;
        color: var(--accent-main);
        font-size: 2.28em;
        letter-spacing: 0.05em;
        line-height: 1;
        user-select: none;
        margin-right: 2.5em;
        display: inline-block;
        transition: color 0.5s;
      }
      .nav-links {
        list-style: none;
        display: flex;
        gap: 2em;
        margin: 0; padding: 0;
      }
      .nav-link {
        color: var(--text-main);
        font-size: 1.12em;
        text-decoration: none;
        font-family: 'Poppins',sans-serif;
        font-weight: 600;
        position: relative;
        transition: color 0.18s;
        padding: 0.15em 0;
      }
      .nav-link:hover,
      .nav-link.active {
        color: var(--accent-main);
      }
      @media (max-width:700px) {
        .nav-content { padding: 0.11em 1em 0.29em 1em;}
        .nav-brand { font-size: 1.2em;}
        .nav-links { gap: 1em;}
      }
      #all-dots-canvas {
        position: fixed;
        top:0; left:0; width:100vw; height:100vh;
        pointer-events: none; z-index:0; opacity:0.38; transition: opacity .7s;
      }
      main {
        max-width: 750px;
        margin: 3em auto;
        background: var(--accent-dark);
        padding: 2.5em 2em 2em 2em;
        border-radius: 20px;
        box-shadow: 0 8px 32px rgba(0,0,0,0.18);
        border: 1.5px solid var(--border-main);
        position: relative;
        z-index: 1;
      }
      h1, h2, h3 {
        font-family: 'Poppins', 'Montserrat', Arial, sans-serif;
        font-weight:700;
        letter-spacing: 0.03em;
        margin-bottom: 0.15em;
      }
      h1 {
        color: var(--accent-main);
        font-size: 2.55rem;
        text-shadow: 1px 1px 6px var(--main-bg);
      }
      h2 {
        color: var(--glow-main);
        font-size: 1.38rem;
        margin-top: 1.28em;
      }
      h3 {
        color: var(--accent-main);
        font-size: 1.12rem;
        margin-top: 1.38em;
      }
      .simple-divider {
        width: 100%;
        height: 2.5px;
        background: var(--accent-main);
        border: 0;
        border-radius: 2px;
        margin: 2em 0 1.4em 0;
        opacity: 0.55;
        transition: background 0.27s;
      }
      ul {
        padding-left: 1.45em;
        line-height: 1.7;
      }
      strong {
        color: var(--accent-main);
        transition: color .5s;
      }
      blockquote {
        border-left: 3px solid var(--accent-main);
        color: var(--text-main);
        padding-left: 1em;
        font-style: italic;
        margin: 1.4em 0;
        opacity: 0.86;
      }
      .pill-list {
        display: flex;
        flex-wrap: wrap;
        gap: 0.62em;
        margin: 0.8em 0 2.2em 0;
      }
      .pill {
        background: var(--main-bg);
        color: var(--text-main);
        border-radius: 99px;
        font-size: 1em;
        padding: 0.32em 1.1em;
        border: 1px solid var(--accent-main);
        opacity: 0.96;
      }
      .software-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit,minmax(180px,1fr));
        gap: 1em 1.25em;
        margin: 1.5em 0 2.2em 0;
        padding:0;
        list-style-type: none;
      }
      .software-card {
        background: var(--main-bg);
        color: var(--accent-main);
        border-radius: 15px;
        font-size: 1.07em;
        padding: 0.55em 1em 0.48em 1em;
        border: 1.5px solid var(--accent-main);
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: bold;
        box-shadow: 0 1.5px 10px #0006;
        height: 2.8em;
      }
      .facts-list {
        line-height: 1.6;
        margin-bottom: 1.8em;
        color: var(--glow-main);
      }
      .facts-list li strong {
        color: var(--accent-main);
      }
      .disclaimer-section {
        max-width:500px;
        margin: 4em auto 1.6em auto;
        text-align:center;
      }
      .disclaimer-title {
        color: var(--accent-main);
        font-family:'Poppins',sans-serif;
        font-size: 1.25em;
        font-weight: 700;
        letter-spacing: 0.03em;
        margin-bottom: 0.1em;
        text-align: center;
      }
      .disclaimer-info {
        background: #1a1027;
        color: #E07BE0;
        border-left: 4px solid #E07BE0;
        border-radius: 5px;
        padding: 0.78em 1.25em 0.94em 1.25em;
        margin: 0 auto 0.2em auto;
        font-size: 1.04em;
        font-family: 'Fira Mono',monospace;
        display: inline-block;
        text-align: left;
        max-width: 95%;
        line-height:1.6;
      }
      .signature {
        font-family: 'Nothing You Could Do', cursive;
        color: var(--accent-main);
        font-size: 2.7em;
        text-align: right;
        display: block;
        margin-top: 2.4em;
        margin-bottom: 0.5em;
        letter-spacing: .06em;
        line-height:1.08;
        user-select: none;
        text-shadow: 0 1px 10px #0007;
        opacity: 1;
        padding-right: .5em;
        cursor: pointer;
        transition: color 0.4s, text-shadow 0.4s;
      }
      @media (max-width:700px) {
        main { padding: 8vw 3vw;}
      }
    </style>
  </head>
  <body>
    <nav class="main-nav">
      <div class="nav-content">
        <span class="nav-brand">Strixly</span>
        <ul class="nav-links">
          <li><a href="/" class="nav-link">Home</a></li>
          <li><a href="/about" class="nav-link active">About</a></li>
          <li><a href="/art" class="nav-link">Art</a></li>
          <li><a href="/photo" class="nav-link">Photography</a></li>
          <li><a href="/video" class="nav-link">Videography</a></li>
        </ul>
      </div>
    </nav>
    <canvas id="all-dots-canvas"></canvas>
    <main>
      <h1>About Me</h1>
      <h2>Will <span style="font-size:1em; color:var(--accent-main);">(Strix/Strixly)</span></h2>
      <div class="simple-divider"></div>
      <p>
        <strong>Hi, I’m Will!</strong> I’m passionate about <strong>creative design</strong>: fine art, photography, graphic design, and film &amp; video production. Raised in England, I’ve created art since I was young—always curious about new ways to express ideas.
      </p>
      <p>
        As a self-taught image editor, I started with free tools like <strong>Pixlr E</strong> and <strong>PicsArt</strong> on my phone. In school and college, I developed my <strong>Adobe Suite</strong> skills for editing, manipulation, digital drawing, and video production. I mostly use <span style="color:var(--accent-main);"><strong>Photoshop</strong></span> for illustration and edits, sometimes <strong>Illustrator</strong> for graphics, and <strong>Premiere Pro</strong> for film/video. I’m learning <strong>Adobe Animate</strong> for animation and use <strong>Procreate</strong> on my iPad for concept art and digital painting.
      </p>
      <p>
        Recent photography and videography have been shot on my Canon EOS 2000D, but I’m now making the most of my iPhone 14 until I can upgrade my camera. I’ve explored traditional mediums forever: pencil/ink drawing, watercolours, oils, acrylics, monoprinting, cyanotypes, and more.
      </p>
      <p>
        I'm always experimenting and love seeing how different media can combine to create unique visual identities. Inspiration comes from nature, surroundings, and even current events or pop culture.
      </p>
      <div class="simple-divider"></div>
      <h3>Education</h3>
      <ul>
        <li><strong>Secondary School:</strong> Art &amp; Design, Design &amp; Technology, Creative iMedia</li>
        <li><strong>College (A Levels &amp; BTEC):</strong> Fine Art, Graphics, Video &amp; Film Production</li>
      </ul>
      <div class="simple-divider"></div>
      <h3>Software I Use</h3>
      <ul class="software-grid">
        <li class="software-card">Adobe Photoshop</li>
        <li class="software-card">Adobe Illustrator</li>
        <li class="software-card">Adobe Premiere Pro</li>
        <li class="software-card">Adobe Lightroom</li>
        <li class="software-card">Adobe Animate</li>
        <li class="software-card">Procreate</li>
        <li class="software-card">Pixlr E</li>
        <li class="software-card">PicsArt</li>
        <li class="software-card">Canon EOS Utility</li>
      </ul>
      <div class="simple-divider"></div>
      <h3>Fun Facts & Things I Enjoy</h3>
      <ul class="facts-list">
        <li><strong>Experimenting</strong> with new art mediums & styles</li>
        <li><strong>Gaming</strong> & all things tech</li>
        <li><strong>Music</strong>, TV &amp; films</li>
        <li>Learning <strong>instruments</strong> (still work in progress!)</li>
        <li>Capturing everyday weirdness through <strong>photo &amp; video</strong></li>
        <li>Nature walks &amp; finding inspiration outdoors</li>
        <li><strong>Pop culture</strong> & keeping up with trends</li>
        <li><strong>Branding</strong> & self-promotion—done all my own work!</li>
      </ul>
      <div class="simple-divider"></div>
      <h3>Collaborations</h3>
      <p>
        I’ve completed photography &amp; videography work for <strong>musician and content creator</strong>
        <span style="color: var(--accent-main);">the.ven0m</span>.
      </p>
      <div style="display:flex; justify-content:center;">
        <a href="https://linktr.ee/the.ven0m?utm_source=linktree_profile_share&ltsid=1749b723-1d27-4b89-ab8d-de2f8620c746"
          class="pill"
          style="margin-bottom:2em; font-weight:bold;"
          target="_blank"
          rel="noopener">
          the.ven0m on Socials
        </a>
      </div>
      <div class="simple-divider"></div>
      <h3 style="text-align:center;">Follow Me</h3>
      <div style="display: flex; flex-wrap: wrap; gap: 1rem; margin-bottom: 2.2em; justify-content: center;">
        <a href="https://www.instagram.com/strixly948"
           class="pill"
           target="_blank" rel="noopener">Instagram</a>
        <a href="https://www.youtube.com/@Strix948"
           class="pill"
           target="_blank" rel="noopener">YouTube</a>
        <a href="https://www.tiktok.com/@strix948"
           class="pill"
           target="_blank" rel="noopener">TikTok</a>
        <a href="https://x.com/strix948"
           class="pill"
           target="_blank" rel="noopener">Twitter/X</a>
        <a href="https://bsky.app/profile/strix948.bsky.social"
           class="pill"
           target="_blank" rel="noopener">Bluesky</a>
      </div>
      <div class="disclaimer-section">
        <div class="disclaimer-title">
          Disclaimer
        </div>
        <div class="disclaimer-info">
          All visual artwork showcased here is human-made. I do not use AI art generators and do not support their commercial use, as it takes away from the creativity and passion of traditional and digital artists.
        </div>
      </div>
      <span class="signature">Strix xoxo</span>
    </main>
    <script>
      // Dynamic glowing dots background
      const canvas = document.getElementById("all-dots-canvas");
      const ctx = canvas.getContext("2d");
      const colorNormal = ["#E07BE0","#392F5A"];
      let w = window.innerWidth, h = window.innerHeight;
      let dots = [];
      function resizeDots() {
        w = window.innerWidth; h = window.innerHeight; canvas.width = w; canvas.height = h;
      }
      window.addEventListener("resize", resizeDots);
      function spawnDots() {
        dots = [];
        let totalDots = Math.floor(w*h/7000);
        for(let i=0;i<totalDots;++i) {
          const x = Math.random() * w, y = Math.random() * h;
          const delay = Math.random()*7.5;
          const size = Math.random()*17+9;
          const life = (Math.random()*6)+4;
          dots.push({
            x,y,size,
            color: colorNormal[Math.floor(Math.random()*colorNormal.length)],
            t: Math.random()*10,
            life, delay
          });
        }
      }
      function animateDots() {
        ctx.clearRect(0,0,w,h);
        let tm = Date.now()/1000;
        for(const dot of dots) {
          let localTime = (tm+dot.delay+dot.t)%dot.life;
          let alpha =
            localTime < 0.3 ? localTime/0.3
            : localTime > dot.life-0.3 ? 1-(localTime-(dot.life-0.3))/0.3
            : 1;
          ctx.beginPath(); ctx.arc(dot.x,dot.y,dot.size,0,Math.PI*2);
          ctx.fillStyle = dot.color.replace(')', `,${0.22*alpha})`).replace('rgb(', 'rgba(');
          ctx.globalAlpha = 0.32*alpha;
          ctx.shadowColor = dot.color;
          ctx.shadowBlur = 6;
          ctx.fill();
          ctx.globalAlpha=1;
          ctx.shadowBlur = 0;
        }
        requestAnimationFrame(animateDots);
      }
      resizeDots();
      spawnDots();
      animateDots();
      window.addEventListener("resize", ()=>{
        resizeDots(); spawnDots();
      });
    </script>
  </body>
</html>
